name: codeql config

# Query set aimed at broad security and quality coverage,
# excluding generated or fixture directories that pollute results.

queries:
  - uses: security-extended
  - uses: security-and-quality

paths-ignore:
  - node_modules
  - dist
  - coverage
  - pacts
  - reports
  - mocks
  - postman
  - openapi
